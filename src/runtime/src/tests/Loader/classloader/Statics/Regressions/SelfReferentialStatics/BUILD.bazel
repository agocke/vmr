load("//src/tests:live_test.bzl", "coreclr_test")

coreclr_test(
    name = "SelfReferentialStatics",
    srcs = ["SelfReferentialStatics.cs"],
    tags = ["manual"],  # Needs additional deps not yet available
)
