load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "MethodsLib",
    srcs = ["MethodsLib.il"],
    tags = ["manual"],  # Needs additional deps not yet available
)

il_coreclr_test(
    name = "MethodsLib2",
    srcs = ["MethodsLib2.il"],
    tags = ["manual"],  # Needs additional deps not yet available
)
