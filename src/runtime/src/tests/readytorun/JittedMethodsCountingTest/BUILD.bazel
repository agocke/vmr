load("//src/tests:live_test.bzl", "coreclr_test")

coreclr_test(
    name = "JittedMethodsCountingTest",
    srcs = ["JittedMethodsCountingTest.cs"],
    optimize = True,
    tags = ["manual"],  # Needs CORE_ROOT env var
)
