load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "Runtime_75312",
    srcs = ["Runtime_75312.il"],
    optimize = True,
    target_compatible_with = ["@platforms//cpu:i386"],  # 32-bit only
)
