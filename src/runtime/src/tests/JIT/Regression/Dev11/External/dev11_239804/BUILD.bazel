load("//src/tests:live_test.bzl", "coreclr_test")

coreclr_test(
    name = "ShowLocallocAlignment",
    srcs = ["ShowLocallocAlignment.cs"],
    pri = 1,
    debug_type = "pdbonly",
    allow_unsafe_blocks = True,
)
