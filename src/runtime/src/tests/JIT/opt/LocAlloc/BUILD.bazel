load("//src/tests:live_test.bzl", "coreclr_test")

coreclr_test(
    name = "localloc",
    srcs = ["localloc.cs"],
    optimize = True,
    allow_unsafe_blocks = True,
)

coreclr_test(
    name = "inloop",
    srcs = ["inloop.cs"],
    optimize = True,
    allow_unsafe_blocks = True,
)
