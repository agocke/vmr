load("//src/tests:live_test.bzl", "coreclr_test")

coreclr_test(
    name = "shared_Enum",
    srcs = ["shared.cs"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
)

coreclr_test(
    name = "hasflag",
    srcs = ["hasflag.cs"],
    debug_type = "pdbonly",
    optimize = True,
)
