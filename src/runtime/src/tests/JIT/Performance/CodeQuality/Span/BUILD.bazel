load("//src/tests:live_test.bzl", "coreclr_test")

coreclr_test(
    name = "SpanBench",
    srcs = ["SpanBench.cs"],
    debug_type = "pdbonly",
    optimize = True,
)

coreclr_test(
    name = "Indexer",
    srcs = ["Indexer.cs"],
    debug_type = "pdbonly",
    optimize = True,
    allow_unsafe_blocks = True,
)
