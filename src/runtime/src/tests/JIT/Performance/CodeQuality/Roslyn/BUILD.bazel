load("//src/tests:live_test.bzl", "coreclr_test")

coreclr_test(
    name = "CscBench",
    srcs = ["CscBench.cs"],
    debug_type = "pdbonly",
    optimize = True,
    tags = ["manual"],  # Requires Microsoft.CodeAnalysis NuGet package
)
