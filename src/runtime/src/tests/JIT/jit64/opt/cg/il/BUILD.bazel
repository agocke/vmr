load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "ldftn_impl",
    srcs = ["ldftn_impl.il", "ldftn.il"],
    pri = 1,
    optimize = True,
)

il_coreclr_test(
    name = "jmp_do",
    srcs = ["jmp_do.il", "jmp.il"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
)

il_coreclr_test(
    name = "ldftn_do",
    srcs = ["ldftn_do.il", "ldftn.il"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
)

il_coreclr_test(
    name = "jmp_impl",
    srcs = ["jmp_impl.il", "jmp.il"],
    pri = 1,
    optimize = True,
)

il_coreclr_test(
    name = "jmp_d",
    srcs = ["jmp_d.il", "jmp.il"],
    pri = 1,
    debug_type = "full",
)

il_coreclr_test(
    name = "ldftn_d",
    srcs = ["ldftn_d.il", "ldftn.il"],
    pri = 1,
    debug_type = "full",
)
