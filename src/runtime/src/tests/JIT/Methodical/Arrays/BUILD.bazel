load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "huge_struct",
    srcs = ["huge_struct.il"],
    pri = 1,
    debug_type = "full",
    optimize = False,
    size = "medium",
)
