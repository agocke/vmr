load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "lcs_ldlen_r",
    srcs = ["lcs_ldlen_r.il", "lcs_ldlen.il"],
    pri = 1,
    debug_type = "pdbonly",
)

il_coreclr_test(
    name = "lcs_ldlen_d",
    srcs = ["lcs_ldlen_d.il", "lcs_ldlen.il"],
    pri = 1,
    debug_type = "full",
)
