load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "rethrow",
    srcs = ["rethrow.il"],
    debug_type = "pdbonly",
    optimize = True,
)

il_coreclr_test(
    name = "finally",
    srcs = ["finally.il"],
    debug_type = "pdbonly",
    optimize = True,
)

il_coreclr_test(
    name = "filter",
    srcs = ["filter.il"],
    debug_type = "pdbonly",
    optimize = True,
)

il_coreclr_test(
    name = "fault_throwbox",
    srcs = ["fault.il"],
    debug_type = "pdbonly",
    optimize = True,
)
