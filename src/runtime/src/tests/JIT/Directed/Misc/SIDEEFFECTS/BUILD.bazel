load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "SideEffects",
    srcs = ["SideEffects.il"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
    size = "medium",
)

il_coreclr_test(
    name = "BadRegArgs",
    srcs = ["BadRegArgs.il"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
    size = "medium",
)
