load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "funcptrtest",
    srcs = ["funcptrtest.il"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
)

il_coreclr_test(
    name = "MutualThdRecur-fptr",
    srcs = ["MutualThdRecur-fptr.il"],
    debug_type = "pdbonly",
)
