load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "add_Desktop_unaligned_1",
    srcs = ["add.il"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
    size = "medium",
)
