load("//src/tests:live_test.bzl", "il_coreclr_test")

il_coreclr_test(
    name = "add_Desktop_volatile",
    srcs = ["add.il"],
    pri = 1,
    debug_type = "pdbonly",
    optimize = True,
    size = "medium",
)
