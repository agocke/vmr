load("//src/tests:live_test.bzl", "coreclr_test")

exports_files(["StructDefinitions.cs"])

coreclr_test(
    name = "nullcomparaison_r",
    srcs = ["nullcomparaison.cs", "StructDefinitions.cs"],
    pri = 1,
)

coreclr_test(
    name = "boxunboxvaluetype_do",
    srcs = ["boxunboxvaluetype.cs", "StructDefinitions.cs"],
    debug_type = "full",
    optimize = True,
)

coreclr_test(
    name = "nullcomparaison_d",
    srcs = ["nullcomparaison.cs", "StructDefinitions.cs"],
    pri = 1,
    debug_type = "full",
    optimize = False,
)

coreclr_test(
    name = "nullcomparaison_ro",
    srcs = ["nullcomparaison.cs", "StructDefinitions.cs"],
    pri = 1,
    optimize = True,
)

coreclr_test(
    name = "boxunboxvaluetype_r",
    srcs = ["boxunboxvaluetype.cs", "StructDefinitions.cs"],
)

coreclr_test(
    name = "nullcomparaison_do",
    srcs = ["nullcomparaison.cs", "StructDefinitions.cs"],
    pri = 1,
    debug_type = "full",
    optimize = True,
)

coreclr_test(
    name = "boxunboxvaluetype_ro",
    srcs = ["boxunboxvaluetype.cs", "StructDefinitions.cs"],
    optimize = True,
)
